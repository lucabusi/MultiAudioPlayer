# Core (richiesti)
PyQt5
numpy
soundfile
Pillow

# Decode waveform — fallback per formati non gestiti da libsndfile (AAC, WMA, ...)
librosa
# matplotlib      # solo per i benchmark legacy (bench_envelope.py), non serve all'app

# Backend audio — almeno uno consigliato.
# Il backend 'qt' (QMediaPlayer) non richiede nulla: è incluso in PyQt5.
# Senza backend funzionante, _StubBackend prende il posto (UI-only, no audio output).
python-vlc
# python-mpv      # alternativa a VLC
# PyGObject       # per GStreamer (richiede anche gstreamer1.0-plugins-base a livello di sistema)
